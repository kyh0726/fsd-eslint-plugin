// Utility functions for rules

/**
 * Example utility function
 * @param {string} path - File path
 * @returns {boolean} Whether the path matches FSD pattern
 */
function isFSDPath(path) {
  // Implement FSD path validation logic
  return false;
}

module.exports = {
  isFSDPath,
};
